# Prepared Blocks Placeholder

Place the full prepared block directory here for real-environment rollouts:

```text
results_real/blocks/
```

The directory should contain township subdirectories with
`block_compositions.json`, `block_features.json`, and related Tool1 outputs.
Place `townships.json` at the repository root.
